{
  "currency": "USD",
  "display_name": "HTTP Relay (Multi-Enrollment)",
  "documents": {
    "Connectivity test": {
      "$doc_preset": "api_connectivity"
    },
    "How to use this service": {
      "category": "getting_started",
      "description": "Guide to setting up multiple HTTP relay enrollments",
      "file_path": "../../docs/relay-description.md",
      "is_active": true,
      "is_public": true,
      "mime_type": "markdown"
    },
    "Python code example": {
      "$doc_preset": "llm_code_example_requests"
    }
  },
  "list_price": {
    "description": "$0.001 per request relayed",
    "price": "0.001",
    "type": "constant"
  },
  "schema": "listing_v1",
  "service_options": {
    "ops_testing_parameters": {
      "base_url_secret": "HTTP_RELAY_BASE_URL",
      "api_key_secret": "HTTP_RELAY_API_KEY"
    }
  },
  "status": "ready",
  "time_created": "2026-04-07T00:00:00Z",
  "user_access_interfaces": {
    "http_gateway": {
      "access_method": "http",
      "base_url": "${API_GATEWAY_BASE_URL}/{{ enrollment_vars.code }}"
    }
  },
  "user_parameters_schema": {
    "type": "object",
    "properties": {
      "base_url_secret": {
        "type": "string",
        "description": "Name of the customer secret containing the upstream base URL"
      },
      "api_key_secret": {
        "type": "string",
        "default": "",
        "description": "Name of the customer secret containing the API key (leave empty if no auth)"
      }
    },
    "required": [
      "base_url_secret"
    ]
  }
}
